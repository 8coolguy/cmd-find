# Create a new git branch from a base branch and switch to it
# Tags: git, branch
# @param branch_name | Branch name | text
# @param base | Base branch | text | main
git checkout -b {{branch_name}} {{base}}
