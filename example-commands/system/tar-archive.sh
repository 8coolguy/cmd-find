# Create a tar.gz archive of a directory
# Tags: system, archive
# @param dir | Directory to archive | path
# @param name | Archive name (without extension) | text | archive
tar -czvf {{name}}.tar.gz {{dir}}
